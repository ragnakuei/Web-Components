const {createApp, ref, reactive, watch, computed, onMounted} = Vue;

createApp({
    components: {
    },
    setup() {

        onMounted(() => {
        });

        return {};
    },
}).mount("#app");
